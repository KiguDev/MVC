<h1>Restaurantes</h1>

<!--<a class="btn btn-primary" onclick="ModalAgregar()" @*asp-action="Agregar"*@>Agregar</a>-->

<table class="table table-bordered" id="tabla-restaurantes-contenido">
    <thead>
        <tr>
            <th hidden>ID</th>
            <th>Logo</th>
            <th>Nombre</th>
            <th>Dirección</th>
            <th>Teléfono</th>
            <th>Página Web</th>
            <th>Fecha Alta</th>
            <th>Hora Cierre</th>
            <th>Empleados</th>
            <th>Mesas</th>
            <th>Opciones</th>
        </tr>
    </thead>
    <tbody>
        @*@foreach (var restaurante in Model)
            {
            <tr>
                <td hidden>@restaurante.Id</td>
                <td><img src="@restaurante.Logo" width="50" height="50"></td>
                <td>@restaurante.Nombre</td>
                <td>@restaurante.Domicilio</td>
                <td>@restaurante.Telefono</td>
                <td>@restaurante.PaginaWeb</td>
                <td>@restaurante.FechaDeAlta.ToString("dd/MM/yyyy")</td>
                <td>@restaurante.HoraDeCierre:00</td>

                <td><a asp-controller="Empleados" asp-action="Index" asp-route-id="@restaurante.Id">@restaurante.empleados.Count()</a></td>
                <td><a asp-action="Mesas" asp-route-id="@restaurante.Id">@restaurante.mesas.Count()</a></td>
                <td>
                    <a asp-action="Editar" asp-route-id="@restaurante.Id" class="fa fa-pencil"></a>
                    <a asp-action="Eliminar" asp-route-id="@restaurante.Id" class="fa fa-trash"></a>
                    <a href="#" class="fa fa-trash ejecutar-eliminacion" data-id="@restaurante.Id"></a>
                </td>
            </tr>
            }*@
    </tbody>
</table>





<div class="modal fade" tabindex="-1" role="dialog" id="agregarModal" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title" id="exampleModalLabel"></h3>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body" id="modalCuerpo">

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" id="btnGuardarRestaurante">Guardar</button>
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cerrar</button>
            </div>
        </div>
    </div>
</div>






@section Scripts{

    <script>

        function LimpiarModales() {
            var bodies = document.getElementsByClassName("modal-body");
            for (i = 0; i < bodies.length; ++i) {
                bodies[i].innerHTML = "";
            }
        }

        window.onload = function () {
            document.getElementById("btnGuardarRestaurante").addEventListener("click", function () {
                GuardarRestaurante()
            });
        };

        function ModalAgregarEditar(id) {
            var url;
            if (id == 0) {
                document.getElementById("exampleModalLabel").innerHTML = "Agregar".bold()
                url = "@Url.Action("Agregar","Home")";
            } else {
                document.getElementById("exampleModalLabel").innerHTML = "Editar".bold()
                url = "@Url.Action("Editar","Home")/" + id;
            }
           
            //var req = new XMLHttpRequest();
            //req.open("GET", "@Url.Action("Agregar","Home")", true);
            //req.open("GET", url, true);
            //req.send();
            //req.onreadystatechange = function () {
                //if (req.status == 200) {
                    //document.getElementById("modalCuerpo").innerHTML = req.responseText;
                    //$("#agregarModal").modal("show");
                //}
            //};

            fetch(url, {
                    method: "GET"
                 }).then(function (response) {
                    if (response.ok) {
                        response.text().then(function (text) {
                            LimpiarModales()
                            document.getElementById("modalCuerpo").innerHTML = text;
                            $("#agregarModal").modal("show");
                        })
                    }
            });

        }

        @*function ModalEditar(id) {
            var req = new XMLHttpRequest();
            req.open("GET", "@Url.Action("Editar","Home")/"+id, true);
            req.send();
            req.onreadystatechange = function () {
                if (req.status == 200) {
                    document.getElementById("modalCuerpo").innerHTML = req.responseText;
                    $("#agregarModal").modal("show");
                }
            };

        }*@

        function GuardarRestaurante() {
            var id = document.getElementById("Id").value;
            var nombre = document.getElementById("Nombre").value;
            var domicilio = document.getElementById("Domicilio").value;
            var telefono = document.getElementById("Telefono").value;
            var logo = document.getElementById("Logo").value;
            var paginaweb = document.getElementById("PaginaWeb").value;
            var horacierre = document.getElementById("HoraDeCierre").value;
            var url;
            var method;

            var fd = {};
                fd["Nombre"] = nombre;
                fd["Domicilio"] = domicilio;
                fd["Telefono"] = telefono;
                fd["Logo"] = logo;
                fd["PaginaWeb"] = paginaweb;
                fd["HoraDeCierre"] = horacierre;

            if (id == 0) {
                url = "/api/restaurantes";
                method = "POST";
            } else {
                url = "/api/restaurantes/" + id;
                method = "PUT";
            }
            fetch(url, {
                method: method,
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(fd)
                }).then(function (response) {
                    if (response.ok) {
                        $("#agregarModal").modal("hide");
                        table.ajax.reload(myCallBack);
                    }
                });

        }
    </script>



    <script>

    /*$('a.ejecutar-eliminacion').click(function (event) {
        event.preventDefault();
        //var resId = $(this).attr('data-id');
        var resId = $(this).data('id');
        var IsConfirmed = confirm("Seguro que desea eliminar el restaurante");
        if (IsConfirmed) {
            $.ajax({
                url: 'https://localhost:44317/api/restaurantes/' + resId,
                type: 'DELETE',
                success: function (result) {
                    //alert('Se elimino el restauratnte');
                    //window.location.reload();
                    recargarElementos
                }
            });
        }
    });*/

    //$('#tabla-restaurantes-contenido').on('click','.ejecutar-eliminacion', function (event) {
    //    event.preventDefault();
    //    var id = $(this).attr('data-id');
    //    var id = $(this).data('id');

    //    bootbox.confirm("Seguro que quieres eliminar este elemento?", function (result) {
    //        if (result) {
    //         eliminar elemento
    //            $.ajax({
    //                url: '/api/restaurantes/' + id,
    //                type: 'DELETE',
    //                success: recargarElementos
    //            });
    //        }
    //    });

        /*if (IsConfirmed) {
            // eliminar elemento
            $.ajax({
                url: 'https://localhost:44343/api/restaurantes/' + id,
                type: 'DELETE',
                success: recargarElementos
            });

        }
    });*/

    @*function recargarElementos() {
        // obtener elementos
        $.getJSON('/api/restaurantes/', function (data) {
            var rows;
            data.forEach(function (element) {
                var row = '<tr>'
                    + '<td>' + '<img src="'+element.logo+'" width="50" height="50">' + '</td>'
                    + '<td class="clase">' + element.nombre + '</td>'
                    + '<td>' + element.domicilio + '</td>'
                    + '<td>' + element.telefono + '</td>'
                    + '<td>' + element.paginaWeb + '</td>'
                    + '<td>' + element.fechaDeAlta + '</td>'
                    + '<td>' + element.horaDeCierre + '</td>'
                    + '<td><a href="@Url.Action("Index","Empleados")/Index/"' + element.id + '">' + element.cantidadEmpleados + '</a></td>'
                    + '<td><a href="@Url.Action("Mesas", "Index")/"' + element.id + '">' + element.cantidadMesas + '</a></td>'
                    + '<td><a href="@Url.Action("Editar")/"' + element.id + '">Editar</a>'
                    + '<a href = "#" data-id="' + element.id + '" class="ejecutar-eliminacion" > Eliminar</a ></td > '
                    + '</tr>';
                rows += row;
            });
            $('#tabla-restaurantes').html(rows);
        });
    }*@

        var myCallBack = function () {
             var buttons = document.getElementsByClassName("fa fa-pencil");
                for (i = 0; i < buttons.length; ++i) {
                    buttons[i].addEventListener("click", function () {
                        ModalAgregarEditar(this.dataset.id);
                    })
                }
        }

        var table;

    $(document).ready( function () {
        table = $('#tabla-restaurantes-contenido').DataTable({
            initComplete: myCallBack,
            ajax: {
                url: 'api/restaurantes',
                dataSrc : ''
            },
            columns: [
                { data: 'id', visible: false },
                {
                    data: 'logo',
                    render: function (data, type, row) {
                        return '<img src="' + data + '"width="50" height="50">';
                    }
                },
                {data:'nombre'},
                {data:'domicilio'},
                {data:'telefono'},
                {data:'paginaWeb' },
                {data:'fechaDeAlta' },
                {data: 'horaDeCierre'},
                {
                    data: 'cantidadEmpleados',
                    render: function (data, type, row) {
                        return '<a href="@Url.Action("Index","Empleados")/Index/' + row.id + '">' + data + '</a>';
                    }
                },
                {
                    data: 'cantidadMesas',
                    render: function (data, type, row) {
                        return '<a href="@Url.Action("Mesas")/' + row.id + '">' + data + '</a>';
                    }
                },

                {
                    data: '',
                    render: function (data, type, row) {
                        //return '<a class="fa fa-pencil" href="@Url.Action("Editar")/' + row.id + '"></a><a class="fa fa-address-card" href="@Url.Action("Perfil")/' + row.id + '"></a>'
                        //return '<a class="fa fa-pencil" onclick="ModalAgregarEditar(' + row.id + ')"></a><a class="fa fa-address-card" href="@Url.Action("Perfil")/' + row.id + '"></a>'
                        return '<a class="fa fa-pencil" data-id="' + row.id + '"></a> <a class="fa fa-address-card" href="@Url.Action("Perfil")/' + row.id + '"></a>'
                    }
                }
            ],
            select: true,
            dom: 'Bfrtip',
            buttons: [
                {
                    text: 'Agregar',
                    action: function () {
                       ModalAgregarEditar(0);
                       //window.location="@Url.Action("Agregar")";
                    }
                },
                {
                    text: 'Eliminar',
                    action: function () {
                        bootbox.confirm("Seguro que quieres eliminar este elemento?", function (result) {
                            if (result) {
                                var rows = table.rows({ selected: true }).data();
                                var ids = [];
                                for (var x = 0; x < rows.length; ++x) {
                                    ids.push(rows[x].id);
                                }

                                fetch('/api/restaurantes/' + ids, {
                                    method: "DELETE",
                                        headers: {
                                            'Content-Type': 'application/json'
                                        }
                                    }).then(function (response) {
                                        if (response.ok) {
                                            table.ajax.reload(myCallBack);
                                        }
                                    });

                               @* $.ajax({
                                    type: 'DELETE',
                                    contentType: 'application/json',
                                    data: '[' + ids.toString() + ']',
                                    url: '/api/restaurantes/',
                                    success: function () {
                                        table.ajax.reload(myCallBack);
                                    }
                                });*@
                            }
                        });
                    }
                },

                'copy','excel','pdf'
            ]
        });

        //table.buttons().container().appendTo($('.col-sm-6:eq(0)', table.table().container()));

    });

    </script>
}

