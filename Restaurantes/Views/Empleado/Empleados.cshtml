@model List<Empleado>

<a asp-action="AgregarEmpleado" asp-route-restaurante="@ViewData["empleadoId"]">Agregar Empleado</a>
<table>
    @foreach (var emp in Model)
    {
    <tr>
        <td>@emp.Nombre</td>
        <td>@emp.Puesto</td>
    </tr>
    <button type="submit" name="Id" value="@emp.Id" asp-action="EliminarEmpleado">Eliminar</button>
    }
</table>