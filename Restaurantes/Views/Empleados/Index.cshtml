@model List<Empleado>


<h1>Empleados de @*@Model.FirstOrDefault().Restaurante.Nombre*@</h1>
<a @*asp-action="Agregar" asp-route-id="@ViewData["resId"]"*@ onclick="MostrarAgregar()" class="btn btn-outline-primary">Agregar Empleado +</a>

<table class="table table-bordered table-hover" id="tabla-empleado-contenido">
    <thead>
        <tr>
            <th>ID</th>
            <th> Nombre </th>
            <th> Puesto </th>
            <th></th>
          


        </tr>
    </thead>
    <tbody>
        @*@foreach (var e in Model)
            {
            <tr>
                <td> @e.Nombre </td>
                <td> @e.Puesto</td>

                <td><a asp-action="Editar" asp-route-id="@e.Id" class="btn btn-warning">Editar</a></td>
                <td>
                    <form asp-action="Eliminar">
                        <button name="Id" value="@e.Id" type="submit" class="btn btn-danger">Eliminar</button>
                    </form>
                </td>
            </tr>
            }*@
    </tbody>
</table>


<div class="modal fade" id="AgregarModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">Agregar</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body" id="modalCuerpo">
                ...
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary" onclick="GuardarEmpleado()">Save changes</button>
            </div>
        </div>
    </div>
</div>






@section Scripts{

    <script>
        function MostrarAgregar() {
            var req = new XMLHttpRequest();
            req.open("GET", "@Url.Action("AgregarM","Empleados")", true);
            req.send();

            req.onreadystatechange = () => {
                if (req.status == 200) {
                    document.getElementById("modalCuerpo").innerHTML = req.responseText;

                    $("#AgregarModal").modal("show");
                }
            }
    }

    function GuardarEmpleado() {
        var nombre = document.getElementById("Nombre").value;
        var puesto = document.getElementById("Puesto").value;
        

        var fd = {};
        fd["Nombre"] = nombre;
        fd["Puesto"] = puesto;
       

        fetch("/Api/Empleado/" + @ViewData["resId"], {
            method: 'POST',
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify(fd)
        }).then(function (response) {
            if (response.ok) {
                $("#AgregarModal").modal("hide");
                table.ajax.reload();
            }
        });

        }

    </script>


    <script>
       

         $('#tabla-empleado-contenido').on('click','.ejecutar-eliminacion', function (event) {
        event.preventDefault();
       
             var id = $(this).data('id');


       

             bootbox.confirm("Deseas eliminar este elemento?", function (IsConfirmed) {
                 if (IsConfirmed) {
                     // eliminar elemento
                     $.ajax({
                         url: 'https://localhost:44358/api/empleado/' + id,
                         type: 'DELETE',
                         success: recargarElementos
                     });
                 }
             });
    });
   

    var table;
    $(document).ready( function () {
        table = $('#tabla-empleado-contenido').DataTable({
          
           
            ajax: {
                url: '/api/empleado/' + @ViewData["resId"],
                dataSrc : ''

            },
            columns: [
                {data: 'id'},
               {
                    data: 'nombre',
                    render: function (data, type, row) {
                        return '<a href="@Url.Action("Inforestaurante")/' + row.id + '">' + data + '</a>'
                    }
                },
                {data: 'puesto'},
               
                {
                    render: (data, type, row) => {
                        return '<a class="btn btn-info" href="@Url.Action("Editar")/' + row.id + '">Editar</a>'
                    }
                }
               
            ],
            select: true,
            dom: 'Bfrtip',
            buttons: [
                'copy', 'excel', 'pdf',
                {
                    text: 'Eliminar',
                    action: function () {

                        var rows = table.rows({ selected: true }).data();
                        var ids = [];
                        for (var x = 0; x < rows.length; x++) {
                            ids.push(rows[x].id)
                        }
                     
                         bootbox.confirm("Deseas eliminar este elemento?", function (IsConfirmed) {
                             if (IsConfirmed) {
                                 // eliminar elemento
                                  $.ajax({
                                        contentType: 'application/json',
                                        url: '/api/empleado/',

                                        data: '[' + ids.toString() + ']',
                                     type: 'DELETE',
                                        success: () => {
                                            table.ajax.reload();
                                        }
                                    });

                            }
                        });
              

                        console.log(ids);
                    }
                }
            ]
        });


      

    } );
    </script>



}